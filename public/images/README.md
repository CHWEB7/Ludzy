# Brand assets

- **`ludzy-logo.png`** — Official **LUDZY** wordmark (white double-outline on black). Header loads this file; bottom-aligned with the animated sound bars (`h-12`). Use a wide PNG (~5:1). Replace this file with your master export if you update the brand lockup.

The **Evening social** hero thumbnail uses **video** — place the file under `public/videos/` (`evening-social.mp4`). See `public/videos/README.md`.
